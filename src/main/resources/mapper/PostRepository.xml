<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lec.spring.repository.PostRepository">

    <insert id="createPost" parameterType="com.lec.spring.domain.Post">
        INSERT INTO post (post_grade, post_text, user_id, reservation_id, post_id, post_picture)
        VALUES (#{postGrade}, #{postText}, #{userId}, #{reservationId}, #{postId}, #{postPicture})
    </insert>

</mapper>